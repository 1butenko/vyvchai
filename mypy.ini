[mypy]
# Check only the source tree
files = src

# Allow namespace packages and avoid duplicate-module errors
namespace_packages = True

# Many third-party libs used here don't provide stubs; ignore missing imports
ignore_missing_imports = True

# Be slightly more permissive by default
follow_imports = silent
